{
  "name": "typescript-native-lsp",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Code plugin: TypeScript 7 native language server with TypeScript 6 fallback",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.22"
  },
  "scripts": {
    "test": "node --test --test-timeout=60000 \"test/*.test.mjs\"",
    "fixtures": "npm install --prefix test/fixtures/ts7 && npm install --prefix test/fixtures/ts6 && npm install --prefix test/fixtures/aliased"
  }
}
